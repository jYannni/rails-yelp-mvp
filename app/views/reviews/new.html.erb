<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :rating, collection: Review::RATING%> <br>
  <%= f.input :content, placeholder: "content" %> <br>
  <%= f.button :submit %>
<% end %>
